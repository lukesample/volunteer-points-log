{
  "type": "sqlite",
  "database": "build/database.db",
  "entities": ["build/entity/**/*.ts"],
  "synchronize": true,
  "logging": false,
  "migrations": ["build/migration/**/*.ts"],
  "subscriptions": ["build/subscription/**/*.ts"]
}
